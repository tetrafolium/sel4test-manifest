inspecode:
  tasks:
    - name: child-repo
      type: shell
      commands:
        main:
          - echo "hello"
          - cat ./report.json
      artifacts:
        - path: ./report.json
      reports:
        - path: ./report.json
